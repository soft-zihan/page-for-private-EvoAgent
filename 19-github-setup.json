{
  "chapters": [
    {
      "id": "github-setup",
      "title": "GitHub 连接实战指南",
      "sections": [
        {
          "title": "完整环境搭建",
          "anchor": "github-setup-env",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 15,
          "blocks": [
            {
              "type": "text",
              "text": "前面的教程逐一深入了每个模块的实现细节。本节换一个视角——从零到一完成完整的端到端部署，体验真实的 GitHub 审查流程。\n\n本节带你从零搭建完整的 EvoAgent 开发环境，包括数据库、缓存、LLM 和 GitHub 集成。所有组件都可以在本地运行，无需购买服务器。"
            },
            {
              "type": "text",
              "text": "**第一步：Docker 启动 PostgreSQL 和 Redis**\n\nEvoAgent 的异步任务队列和持久化存储依赖 PostgreSQL 和 Redis。使用 Docker 可以快速启动这两个服务："
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 启动 PostgreSQL：用作任务持久化存储（替代开发模式的 SQLite）\ndocker run -d --name evoagent-postgres \\\n  -e POSTGRES_USER=evoagent \\          # 数据库用户名，对应 EVOAGENT_DATABASE_URL 中的用户\n  -e POSTGRES_PASSWORD=evoagent-local \\  # 数据库密码，仅本地开发使用\n  -e POSTGRES_DB=evoagent \\              # 数据库名称\n  -p 5432:5432 \\                         # 将容器 5432 端口映射到本机，供 EvoAgent 连接\n  postgres:16                            # 使用 PostgreSQL 16 官方镜像\n\n# 启动 Redis：用作异步任务队列的消息中间件（替代开发模式的内存队列）\ndocker run -d --name evoagent-redis \\\n  -p 6379:6379 \\                         # 将容器 6379 端口映射到本机\n  redis:7                                # 使用 Redis 7 官方镜像\n\n# 验证：确认两个容器都在正常运行\ndocker ps",
                "caption": "Docker 启动 PostgreSQL 和 Redis"
              }
            },
            {
              "type": "text",
              "text": "**第二步：Python 虚拟环境**"
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 创建虚拟环境并安装依赖\npython3 -m venv .venv  # 在项目根目录创建名为 .venv 的虚拟环境，隔离 Python 依赖\nsource .venv/bin/activate  # Windows: .venv\\Scripts\\activate  # 激活虚拟环境\npip install -r requirements.txt  # 安装项目依赖（仅 5 个生产包：psycopg、redis、PyJWT、opentelemetry、prometheus）",
                "caption": "创建 Python 虚拟环境"
              }
            },
            {
              "type": "text",
              "text": "**第三步：配置 .env 文件**\n\n在项目根目录创建 `.env` 文件，填入以下配置："
            },
            {
              "type": "code",
              "code": {
                "language": "env",
                "content": "# 服务监听配置\nEVOAGENT_HOST=127.0.0.1  # 监听地址，127.0.0.1 表示仅本机可访问；Docker 中需设为 0.0.0.0\nEVOAGENT_PORT=8080  # HTTP 服务端口，Dashboard 和 API 都通过这个端口访问\n\n# 认证配置\nEVOAGENT_AUTH_REQUIRED=true  # 是否开启 API 认证，生产环境建议设为 true\nEVOAGENT_AUTH_SECRET=local-dev-secret-change-in-production  # JWT 签名密钥，至少 32 字节，生产环境必须更换\nEVOAGENT_BOOTSTRAP_ADMIN_USERNAME=admin  # 初始管理员用户名，首次登录后建议修改\nEVOAGENT_BOOTSTRAP_ADMIN_PASSWORD=admin123456  # 初始管理员密码，生产环境必须更换为强密码\n\n# LLM 配置 - 以阿里云通义千问为例（支持任何 OpenAI 兼容 API）\nEVOAGENT_LLM_PROVIDER=custom  # LLM 提供商类型：custom 表示自定义 OpenAI 兼容端点\nEVOAGENT_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1  # API 端点地址\nEVOAGENT_LLM_API_KEY=你的API-Key  # LLM 服务的 API 密钥\nEVOAGENT_LLM_MODEL=qwen3.7-plus  # 使用的模型名称\n\n# PostgreSQL & Redis 连接配置\nEVOAGENT_DATABASE_URL=postgresql://evoagent:evoagent-local@localhost:5432/evoagent  # PostgreSQL 连接串，格式：postgresql://用户:密码@主机:端口/库名\nEVOAGENT_REDIS_URL=redis://localhost:6379/0  # Redis 连接串，格式：redis://主机:端口/数据库编号\nEVOAGENT_ASYNC_WORKERS=4  # 异步 Worker 数量，决定同时处理多少个审查任务\n\n# Skills 配置\nEVOAGENT_SKILLS_DIR=skills  # Skill 目录路径，系统会扫描此目录加载自定义审查规则\nEVOAGENT_SKILL_SANDBOX=true  # 是否启用 Skill 沙箱隔离（子进程 + 审计钩子），生产环境必须为 true\n\n# GitHub Webhook 配置\nEVOAGENT_GITHUB_WEBHOOK_SECRET=一个随机字符串  # Webhook 签名密钥，必须与 GitHub Webhook 设置中的 Secret 完全一致\nEVOAGENT_GITHUB_TOKEN=你的GitHub-Token  # GitHub 个人访问令牌，用于读取 PR diff 和回写审查评论\nEVOAGENT_AUTO_POST_REVIEW=true  # 审查完成后是否自动将报告发布到 PR 评论区\n\n# 执行预算配置（LLM 审查需要足够时间）\nEVOAGENT_TIMEOUT_SECONDS=300  # 整个审查流程的超时时间（秒），LLM 调用较慢时建议 300 以上\nEVOAGENT_MAX_STEPS=16  # 运行时最大步骤数，多 Agent 协作需要更多步骤\nEVOAGENT_COLLABORATION_ROUNDS=1  # Agent 协作轮次，设为 1 可减少 LLM 调用次数",
                "caption": "完整的 .env 配置模板"
              }
            },
            {
              "type": "text",
              "text": "**关键配置说明：**\n\n- `EVOAGENT_LLM_*`：LLM 配置，支持任何 OpenAI 兼容 API（阿里云 DashScope、DeepSeek、OpenRouter 等）\n- `EVOAGENT_GITHUB_WEBHOOK_SECRET`：Webhook 签名密钥，需要与 GitHub Webhook 配置中的 Secret 完全一致\n- `EVOAGENT_GITHUB_TOKEN`：GitHub Token，用于读取 PR diff 和回写评论\n- `EVOAGENT_AUTO_POST_REVIEW=true`：审查完成后自动将报告发布到 PR 评论区\n- `EVOAGENT_TIMEOUT_SECONDS=300`：整个审查流程的超时时间，LLM 调用较慢时建议设为 300 秒以上\n- `EVOAGENT_MAX_STEPS=16`：运行时最大步骤数，多 Agent 协作需要更多步骤\n- `EVOAGENT_COLLABORATION_ROUNDS=1`：协作轮次，设为 1 可减少 LLM 调用次数"
            }
          ]
        },
        {
          "title": "GitHub Token 配置",
          "anchor": "github-setup-token",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 10,
          "blocks": [
            {
              "type": "text",
              "text": "EvoAgent 需要 GitHub Token 来完成两件事：(1) 读取私有仓库的 PR diff；(2) 将审查报告写回 PR 评论区。Token 的权限配置决定了 EvoAgent 能访问哪些仓库。"
            },
            {
              "type": "text",
              "text": "**场景说明：大小号协作**\n\n一个常见场景是：你有一个主账号（存放私有仓库）和一个小号（用于自动化操作）。小号的 Token 被配置给 EvoAgent，通过 GitHub 的仓库协作权限访问主账号的私有仓库。"
            },
            {
              "type": "list",
              "list": {
                "style": "ordered",
                "items": [
                  {
                    "content": "在主账号仓库中，进入 Settings → Collaborators → Add people，添加小号为协作者（至少 Read 权限）"
                  },
                  {
                    "content": "在小号账号中，进入 Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token"
                  },
                  {
                    "content": "Token 名称填 EvoAgent，Expiration 建议 90 天"
                  },
                  {
                    "content": "勾选权限：repo（完整仓库访问）、write:discussion、read:org"
                  },
                  {
                    "content": "点击 Generate token，复制以 ghp_ 开头的字符串"
                  },
                  {
                    "content": "将 Token 填入 .env 的 EVOAGENT_GITHUB_TOKEN"
                  }
                ]
              }
            },
            {
              "type": "text",
              "text": "**注意：** 如果使用 Fine-grained Token（以 github_pat_ 开头），需要在 Repository access 中选择 Only select repositories，并指定目标仓库。Classic Token（以 ghp_ 开头）则自动继承账号的所有仓库权限。"
            },
            {
              "type": "text",
              "text": "**Token 加载机制**：EvoAgent 使用 `load_dotenv()` 加载 `.env` 文件，内部调用 `os.environ.setdefault()`，不会覆盖已存在的环境变量。因此推荐在启动命令中显式 export 关键变量："
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 推荐：显式 export Token 确保加载（load_dotenv 不会覆盖已存在的环境变量）\nexport EVOAGENT_GITHUB_TOKEN=ghp_xxxxx  # 将 ghp_xxxxx 替换为你的真实 GitHub Token\npython -m evoagent  # 以模块方式启动 EvoAgent 服务",
                "caption": "显式 export Token 后启动服务"
              }
            }
          ]
        },
        {
          "title": "公网隧道：localtunnel",
          "anchor": "github-setup-tunnel",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 5,
          "blocks": [
            {
              "type": "text",
              "text": "GitHub Webhook 需要一个公网可访问的 HTTPS 地址。本地开发时，可以使用隧道工具将本机端口暴露到公网。"
            },
            {
              "type": "code",
              "code": {
                "language": "text",
                "content": "GitHub  # GitHub 服务器，PR 事件的发生地\n↓ HTTPS  # GitHub 通过 HTTPS POST 请求发送 Webhook 事件\nhttps://your-tunnel.loca.lt/webhooks/github  # 隧道提供的公网地址，填入 GitHub Webhook 配置中\n↓ 隧道转发  # localtunnel 将公网请求转发到本机对应端口\nhttp://127.0.0.1:8080/webhooks/github  # EvoAgent 本地监听的 Webhook 接收端点",
                "caption": "隧道架构：GitHub → 公网地址 → 本机服务"
              }
            },
            {
              "type": "text",
              "text": "使用 localtunnel（基于 Node.js，无需额外安装）："
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 启动隧道（固定子域名）\nnpx localtunnel --port 8080 --subdomain your-name\n\n# 成功后访问以下地址确认隧道正常\ncurl https://your-name.loca.lt/health\n# 应返回：{\"status\": \"ok\", ...}",
                "caption": "启动 localtunnel 并验证"
              }
            },
            {
              "type": "text",
              "text": "**注意事项：**\n\n- localtunnel 首次访问可能需要点击确认页面（输入隧道 IP）\n- 子域名可能被占用，可以换用其他名称\n- 隧道进程关闭后地址失效，需要重新配置 GitHub Webhook\n- 生产环境建议使用固定域名或服务器"
            }
          ]
        },
        {
          "title": "Fork 工作流与 PR 创建",
          "anchor": "github-setup-fork",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 10,
          "blocks": [
            {
              "type": "text",
              "text": "测试 Webhook 时，不建议直接在主仓库操作。推荐使用 Fork 工作流：小号 Fork 主仓库，在小号仓库上创建分支和 PR，PR 的目标指向主仓库。"
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 1. 在小号 GitHub 页面 Fork 主仓库\n#    例如：Fork soft-zihan/EvoAgent → Catherguo/EvoAgent\n\n# 2. 本地添加 fork 为远程仓库，这样就能推送到小号的仓库\ngit remote add fork https://github.com/Catherguo/EvoAgent.git\n\n# 3. 创建测试分支并推送（包含故意写的安全漏洞用于测试 Webhook）\ngit checkout -b test/webhook-demo  # 创建并切换到测试分支\necho \"# test file\" > test_vulnerable.py  # 创建一个包含漏洞的测试文件\ngit add test_vulnerable.py  # 将测试文件加入暂存区\ngit commit -m \"test: webhook demo\"  # 提交更改\ngit push fork test/webhook-demo  # 推送到小号仓库的远程分支\n\n# 4. 通过 GitHub API 创建 PR（目标为主仓库，触发 Webhook）\ncurl -X POST \\  # 使用 curl 发送 POST 请求创建 PR\n  -H \"Authorization: token ghp_xxxxx\" \\  # 用小号 Token 认证\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"title\": \"Test: Webhook demo with security issues\",\n    \"head\": \"Catherguo:test/webhook-demo\",  # PR 来源：小号仓库的测试分支\n    \"base\": \"main\",  # PR 目标：主仓库的 main 分支\n    \"body\": \"Testing EvoAgent webhook integration\"\n  }' \\\n  https://api.github.com/repos/soft-zihan/EvoAgent/pulls  # 向主仓库的 pulls 接口提交 PR",
                "caption": "Fork 工作流：创建分支、推送、创建 PR"
              }
            },
            {
              "type": "text",
              "text": "**为什么用 Fork 工作流？**\n\n- 测试文件不会污染主仓库\n- 小号有权限创建分支和 PR\n- PR 的 Webhook 会发送到主仓库配置的地址\n- 审查评论也会以小号身份发布到主仓库 PR 上"
            }
          ]
        },
        {
          "title": "配置 GitHub Webhook",
          "anchor": "github-setup-webhook",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 8,
          "blocks": [
            {
              "type": "text",
              "text": "在**主仓库**（PR 的目标仓库）中进入 Settings → Webhooks → Add webhook，填写以下信息："
            },
            {
              "type": "code",
              "code": {
                "language": "text",
                "content": "Payload URL:  # Webhook 接收地址，GitHub 在 PR 事件时会向这个 URL 发送 POST 请求\nhttps://your-name.loca.lt/webhooks/github  # 替换为你的隧道地址 + /webhooks/github 路径\n（替换为你的隧道地址）\n\nContent type:  # 请求体格式，必须设为 application/json\napplication/json\n\nSecret:  # 签名密钥，EvoAgent 用它验证请求确实来自 GitHub（防止伪造请求）\n与 .env 中 EVOAGENT_GITHUB_WEBHOOK_SECRET 完全相同的值  # 两边必须完全一致，否则签名验证失败返回 401\n\n事件选择：  # 选择要监听的事件类型，只关注 PR 事件即可\nLet me select individual events → Pull requests  # 仅订阅 Pull requests 事件（opened/synchronize/reopened）",
                "caption": "GitHub Webhook 配置"
              }
            },
            {
              "type": "text",
              "text": "**生成 Webhook Secret 的推荐方式：**"
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 生成随机密钥\npython3 -c \"import secrets; print(secrets.token_urlsafe(32))\"\n# 输出示例：ASVrny1PZ8+fzdokTjrSmNYCjjGoJh7XpInqDCv6dkc=",
                "caption": "生成 Webhook Secret"
              }
            }
          ]
        },
        {
          "title": "端到端测试：从推送到审查",
          "anchor": "github-setup-e2e-test",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 15,
          "blocks": [
            {
              "type": "text",
              "text": "现在让我们完成一次完整的端到端测试。确保以下服务都在运行："
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 终端 1：启动 EvoAgent 服务（需要先确保 PostgreSQL 和 Redis 已运行）\nexport EVOAGENT_GITHUB_TOKEN=ghp_xxxxx  # 显式设置 Token，确保 load_dotenv 不会覆盖\npython -m evoagent  # 以模块方式启动，输出启动信息\n# 输出：EvoAgent dashboard: http://127.0.0.1:8080  # 访问此地址打开管理面板\n\n# 终端 2：启动隧道（需要另开一个终端窗口）\nnpx localtunnel --port 8080 --subdomain your-name  # 将本机 8080 端口暴露到公网\n\n# 验证隧道是否正常工作\ncurl https://your-name.loca.lt/health  # 请求健康检查端点\n# 应返回：{\"status\": \"ok\", ...}  # 返回 ok 表示 EvoAgent 服务正常且隧道连通",
                "caption": "启动所有服务"
              }
            },
            {
              "type": "text",
              "text": "创建一个包含安全漏洞的测试文件，推送到 fork 仓库触发 Webhook："
            },
            {
              "type": "code",
              "code": {
                "language": "python",
                "content": "# test_vulnerable.py — 故意包含多种安全漏洞\n\nDATABASE_PASSWORD = \"super_secret_password_123\"  # CWE-798: 硬编码密码\nAPI_KEY = \"sk-1234567890abcdef\"                  # CWE-798: 硬编码 API Key\n\n# dangerous_eval：公开方法，实现核心业务逻辑\ndef dangerous_eval(user_input):\n    result = eval(user_input)  # CWE-95: 代码注入\n    return result\n\n# sql_injection_vulnerable：公开方法，实现核心业务逻辑\ndef sql_injection_vulnerable(username):\n    query = f\"SELECT * FROM users WHERE username = '{username}'\"  # CWE-89: SQL 注入\n    return query\n\n# shell_injection：公开方法，实现核心业务逻辑\ndef shell_injection(user_data):\n    cmd = f\"echo {user_data}\"\n    return subprocess.run(cmd, shell=True, capture_output=True)  # CWE-78: 命令注入\n\n# debug_print_sensitive：公开方法，实现核心业务逻辑\ndef debug_print_sensitive():\n    print(f\"DEBUG: Password is {DATABASE_PASSWORD}\")  # CWE-532: 日志泄露敏感信息\n    # 函数调用完成具体操作",
                "caption": "测试文件：包含 6 种常见安全漏洞"
              }
            },
            {
              "type": "code",
              "code": {
                "language": "bash",
                "content": "# 推送新提交触发 Webhook（模拟开发者在 Fork 仓库上提交代码变更）\necho \"# trigger $(date +%s)\" >> test_vulnerable.py  # 在测试文件中追加一行，制造变更\ngit add test_vulnerable.py                            # 将修改加入 Git 暂存区\ngit commit -m \"test: trigger webhook\"                 # 提交变更（commit message 用于标识测试用途）\ngit push fork test/webhook-demo                       # 推送到 Fork 远程分支，触发 GitHub Webhook 事件",
                "caption": "推送提交触发 Webhook"
              }
            },
            {
              "type": "text",
              "text": "推送后，GitHub 会向 Webhook URL 发送 `push` 事件。观察 EvoAgent 终端日志："
            },
            {
              "type": "code",
              "code": {
                "language": "text",
                "content": "127.0.0.1 - \"POST /webhooks/github HTTP/1.1\" 202 -\n# 202 表示 EvoAgent 已接收任务并异步处理",
                "caption": "EvoAgent 日志：Webhook 接收成功"
              }
            },
            {
              "type": "text",
              "text": "等待 1-3 分钟后（取决于 LLM 响应速度），审查结果会自动发布到 PR 评论区。一次成功的审查结果如下："
            },
            {
              "type": "code",
              "code": {
                "language": "text",
                "content": "# EvoAgent PR Review — #1  # 报告标题，显示 PR 编号\n\n**Repository:** soft-zihan/EvoAgent | **PR:** #1  # 仓库名和 PR 编号\n**Risk:** HIGH | **Findings:** 14 actionable issues  # 整体风险等级为高，共发现 14 个问题\n\n## 🔴 Critical Issues  # 严重问题列表，需要立即修复\n\n1. **Hardcoded database password in source code** (CWE-798)  # 第 1 个问题：硬编码数据库密码\n   - test_vulnerable.py:10  # 问题所在文件和行号\n   - Evidence: DATABASE_PASSWORD = \"super_secret_password_123\"  # 证据：代码中的明文密码\n   - Fix: Remove hardcoded credentials and use environment variables\n     or a secure secrets management system like AWS Secrets Manager.  # 修复建议\n\n2. **Hardcoded API key in source code** (CWE-798)  # 第 2 个问题：硬编码 API 密钥\n   - test_vulnerable.py:11\n   - Evidence: API_KEY = \"sk-1234567890abcdef\"\n   - Fix: Remove hardcoded API keys and use environment variables.\n     Rotate the exposed API key immediately.  # 已暴露的密钥需要立即轮换\n\n3. **Arbitrary code execution via eval()** (CWE-95)  # 第 3 个问题：eval() 代码注入\n   - test_vulnerable.py:15\n   - Evidence: result = eval(user_input)\n   - Fix: Avoid using eval() entirely. Use ast.literal_eval() for\n     simple data structures.  # 建议用 ast.literal_eval() 安全替代\n\n4. **SQL injection vulnerability** (CWE-89)  # 第 4 个问题：SQL 注入\n   - test_vulnerable.py:21\n   - Evidence: f\"SELECT * FROM users WHERE username = '{username}'\"\n   - Fix: Use parameterized queries: conn.execute(\"... WHERE username = ?\", (username,))\n\n5. **Command injection via shell=True** (CWE-78)  # 第 5 个问题：命令注入\n   - test_vulnerable.py:27\n   - Evidence: subprocess.run(cmd, shell=True, capture_output=True)\n   - Fix: Remove shell=True, pass command as list of arguments.  # 用列表传参避免注入\n\n6. **Dynamic code execution may lead to injection** (SEC-EVAL)  # 第 6 个问题：动态代码执行\n   - test_vulnerable.py:15\n   - Evidence: result = eval(user_input)\n   - Fix: 移除动态执行；使用显式解析器或严格白名单。\n\n## 🟡 High Issues  # 高危问题列表\n\n7. **疑似硬编码凭据** (SEC-HARDCODED-SECRET)  # 规则引擎检测到的硬编码密钥\n8. **Shell 调用存在命令注入风险** (SEC-SUBPROCESS-SHELL) × 2  # 两处 shell=True 调用\n9. **Sensitive information logged in debug output** (CWE-532)  # 调试输出泄露敏感信息\n\n## 🟢 Low Issues  # 低危问题列表\n\n10-14. **新增调试输出** (REL-DEBUG-PRINT) × 5  # 5 处 print 调试语句\n\n---\nReviewer: multi-agent-collaboration  # 审查由多 Agent 协作完成\nProtocol: plan-challenge-revise-evidence-verify-arbitrate  # 使用的协作协议流程\nAgents: security-agent, reliability-agent, custom:qwen3.7-plus, code-quality  # 参与的 Agent 列表",
                "caption": "实际审查结果：14 个安全发现（6 Critical + 4 High + 4 Low）"
              }
            },
            {
              "type": "text",
              "text": "审查评论以 Token 所属账号（小号）的身份发布到 PR 评论区，包含完整的漏洞列表、证据、修复建议和测试方法。"
            }
          ]
        },
        {
          "title": "排查 Webhook 问题",
          "anchor": "github-setup-troubleshoot",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 10,
          "blocks": [
            {
              "type": "text",
              "text": "进入 GitHub Settings → Webhooks → 你的 Webhook → Recent Deliveries，查看投递结果："
            },
            {
              "type": "table",
              "table": {
                "headers": [
                  "HTTP 状态码",
                  "含义",
                  "解决方法"
                ],
                "rows": [
                  [
                    "202",
                    "EvoAgent 已接收",
                    "正常，等待异步任务完成"
                  ],
                  [
                    "401",
                    "Secret 不一致",
                    "检查 EVOAGENT_GITHUB_WEBHOOK_SECRET 与 Webhook Secret 是否完全相同"
                  ],
                  [
                    "503",
                    "未配置 Webhook Secret",
                    "启动 EvoAgent 时设置 EVOAGENT_GITHUB_WEBHOOK_SECRET"
                  ],
                  [
                    "500",
                    "Token/权限/Diff 下载失败",
                    "检查 GitHub Token 权限、仓库访问权限"
                  ]
                ]
              }
            },
            {
              "type": "text",
              "text": "**常见问题排查：**"
            },
            {
              "type": "text",
              "text": "**问题 1：私有仓库 diff 获取失败（404）**\n\nGitHub 对私有仓库的 diff 获取需要使用 API URL 而非网页 URL。Webhook payload 中的 `diff_url` 格式为 `https://github.com/owner/repo/pull/1.diff`，这对公开仓库有效，但私有仓库需要转换为 `https://api.github.com/repos/owner/repo/pulls/1` 并附带 Token 认证。EvoAgent 的 `fetch_diff()` 方法已内置了这个转换逻辑。"
            },
            {
              "type": "text",
              "text": "**问题 2：task execution budget exceeded**\n\nLLM 审查超时。解决方案：(1) 增大 `EVOAGENT_TIMEOUT_SECONDS`（建议 300）；(2) 增大 `EVOAGENT_MAX_STEPS`（建议 16）；(3) 减少 `EVOAGENT_COLLABORATION_ROUNDS`（设为 1 可减少一半 LLM 调用）。"
            },
            {
              "type": "text",
              "text": "**问题 3：Token 未加载**\n\n如果日志显示 GitHub API 返回 404，可能是 Token 未正确加载。检查方法：启动服务时显式 `export EVOAGENT_GITHUB_TOKEN=xxx`，因为 `load_dotenv()` 不会覆盖已存在的环境变量。"
            },
            {
              "type": "text",
              "text": "**问题 4：隧道地址变化**\n\nlocaltunnel / cloudflared 重启后地址会变化。需要同步更新 GitHub Webhook 配置中的 Payload URL。"
            },
            {
              "type": "text",
              "text": "**查看审查结果的位置：**\n\n1. EvoAgent Dashboard：http://127.0.0.1:8080 的任务中心\n2. 启动 EvoAgent 的终端日志\n3. GitHub PR 评论区（配置了 `EVOAGENT_AUTO_POST_REVIEW=true` 且 Token 有权限）\n4. 数据库查询：`SELECT id, state, error FROM tasks ORDER BY created_at DESC;`"
            }
          ]
        },
        {
          "title": "完整 .env 参考配置",
          "anchor": "github-setup-full-env",
          "groupTitle": "GitHub 连接实战指南",
          "difficulty": "beginner",
          "estimatedMinutes": 3,
          "blocks": [
            {
              "type": "text",
              "text": "完整的 .env 配置参考已在上方第三步中详细列出。所有环境变量的含义和默认值可以在 evoagent/config.py 的 Settings dataclass 中找到完整定义。"
            }
          ]
        }
      ]
    }
  ]
}